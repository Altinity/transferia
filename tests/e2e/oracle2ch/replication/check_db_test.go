package replication

import "testing"

func TestReplicationSmoke(t *testing.T) {
	t.Skip("blocked: oracle2ch local smoke is not wired yet; requires Oracle test container/image and initialization recipe. See ../README.md")
}
