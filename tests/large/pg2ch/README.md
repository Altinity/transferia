# pg2ch large tests

This directory is reserved for heavy pg2ch-only scenarios (load, soak, failure injection).
Current large suites are still under `tests/large/docker-compose` and will be moved here incrementally.
