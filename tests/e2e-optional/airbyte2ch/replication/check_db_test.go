package replication

import "testing"

func TestReplicationSmoke(t *testing.T) {
	t.Skip("blocked: airbyte2ch local smoke is not wired yet; requires deterministic Airbyte source connector container and fixture. See ../README.md")
}
